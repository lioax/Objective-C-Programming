#include <stdio.h>

int main(int argc, const char * argv[])
{
    float *myPointer; // set pointer to NULL for the moment. An address will be stored there later.
    myPointer = NULL;
    
    // Has myPointer been set?
    if (myPointer) {
        // myPointer is not NULL
        
            } else {
                // myPointer is NULL
            }
    return 0;
}